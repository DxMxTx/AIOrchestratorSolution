{
  "schema": 1,
  "type": "prompt_template",
  "description": "Modifica código HTML y JavaScript existente basándose en una petición del usuario para refinarlo o añadirle funcionalidades.",
  "template_format": "semantic-kernel",
  "input_variables": [
    {
      "name": "existing_code",
      "description": "El bloque de código actual (HTML/JS) que necesita ser modificado.",
      "required": true
    },
    {
      "name": "modification_request",
      "description": "La instrucción del usuario describiendo el cambio o la adición deseada.",
      "required": true
    }
  ],
  "execution_settings": {
    "default": {
      "max_tokens": 2500,
      "temperature": 0.1,
      "top_p": 1.0,
      "presence_penalty": 0.0,
      "frequency_penalty": 0.0
    }
  }
}
